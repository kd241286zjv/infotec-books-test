# Replace with the actual Yii2 backend URL used for local development.
# This value is only an example; it is not a production backend URL.
VITE_API_PROXY_TARGET=http://localhost:8000

# Optional production API base URL. Leave unset to use /api/v1.
# VITE_API_BASE_URL=https://your-backend.example/api/v1
